import { MemberStatePipe } from './member-state.pipe';

describe('MemberStatePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberStatePipe();
    expect(pipe).toBeTruthy();
  });
});
